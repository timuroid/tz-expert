"""
PromptBuilder package.

Назначение:
- отдельный сервис, который собирает готовые messages для LLM
  по входу (markdown + gg_id).
- НЕ вызывает LLM и НЕ агрегирует ответы — это делает внешний оркестратор.

Зависимости:
- использует БД и ORM-модели из проекта tz_expert (не дублируем код).
"""
